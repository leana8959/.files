{
	"Open, read and save file to variable": {
		"prefix": "open",
		"body": [
			"${1:var} = []",
			"with open(\"${2:file.txt}\") as f:",
			"\t${1:var} = [",
			"\t\tline.strip()",
			"\t\tfor line in f.readlines() if line != '\\n'",
			"\t]",
			""
		]
	}
}
